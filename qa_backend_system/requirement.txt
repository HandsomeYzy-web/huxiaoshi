# ==========================================
# 1. 核心 Web 框架与服务器
# ==========================================
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart>=0.0.9      # 处理 multipart/form-data 请求（如文件上传）

# ==========================================
# 2. 数据库驱动与 ORM
# ==========================================
sqlalchemy>=2.0.30           # MySQL 的 ORM 框架
pymysql>=1.1.1               # MySQL 底层驱动
pymilvus>=2.4.0              # 向量数据库 Milvus 的官方驱动

# ==========================================
# 3. 大模型与 AI 编排 (LangChain 生态)
# ==========================================
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0   # 第三方集成包（包含通义千问等）
langchain-milvus>=0.1.0      # LangChain 的 Milvus 专属支持包
langchain-openai>=0.2.0      # (保留备用) OpenAI 兼容模式接口

# ==========================================
# 4. 阿里云通义千问官方 SDK
# ==========================================
dashscope>=1.20.0

# ==========================================
# 5. 基础工具与企业级基建
# ==========================================
python-dotenv>=1.0.1         # 读取 .env 环境变量文件
pydantic>=2.7.0              # 数据格式验证 (FastAPI 核心依赖)
pydantic-settings>=2.0.0     # 从 .env 加载配置
email-validator>=2.1.0       # Pydantic EmailStr 支持
python-jose[cryptography]>=3.3.0  # JWT 生成与验证
bcrypt>=4.0.0                # 密码哈希
requests>=2.32.0             # 发送 HTTP 请求
httpx>=0.27.0                # 异步 HTTP 客户端 (Reranker / TestClient)
loguru>=0.7.2                # 日志系统
rich>=13.7.1                 # 终端美化与彩色横幅打印

# ==========================================
# 6. 新架构核心依赖 (Agent & 数据处理)
# ==========================================
langgraph>=0.0.30            # 用于构建 Agent 智能体的状态机与多步路由流
unstructured[all-docs]>=0.13.0 # 文档解析器 (精准解析 PDF/Word/Excel)
unstructured-inference        # unstructured hi_res 策略依赖（PDF 高精度解析）
cryptography>=42.0.0         # 用于对外部业务数据库的密码进行对称加密存储
tiktoken>=0.6.0              # 用于精确计算大模型 Token 消耗量

# ==========================================
# 7. 对象存储与缓存消息队列 (新增)
# ==========================================
minio>=7.2.0                 # 与 MinIO 交互，存储文件和提取出的图片
redis>=5.0.0                 # 推荐：用于缓存业务数据或作为异步任务队列的 Broker
celery>=5.3.0                # 推荐：处理耗时的文档解析与向量化入库任务

# ==========================================
# 8. 视觉与底层解析支持
# ==========================================
pdf2image>=1.17.0            # 配合 unstructured 处理 PDF 图像提取
pillow>=10.2.0               # 图像处理基础库
pytesseract>=0.3.10          # Tesseract OCR Python 绑定（需安装 tesseract-ocr 系统包）

# ==========================================
# 9. 各文件格式解析依赖
# ==========================================
python-docx>=1.1.0           # DOCX 文件解析
python-pptx>=0.6.23          # PPTX 文件解析
openpyxl>=3.1.0              # XLSX 文件解析
xlrd>=2.0.0                  # XLS（旧版 Excel）文件解析
pdfminer.six>=20221105       # PDF 文本提取
beautifulsoup4>=4.12.0       # HTML 文件解析
lxml>=5.0.0                  # HTML/XML 解析加速引擎
docutils>=0.20               # RST (reStructuredText) 文件解析
striprtf>=0.0.26             # RTF 文件解析
ebooklib>=0.18               # EPUB 电子书解析

# ==========================================
# 10. 测试框架
# ==========================================
pytest>=8.0.0
pytest-asyncio>=0.23.0